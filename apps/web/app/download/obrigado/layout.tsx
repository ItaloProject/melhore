import type { ReactNode } from 'react'

export const metadata = {
  title: 'Download iniciado',
  description: 'Obrigado por baixar o Melhore.',
}

export default function DownloadObrigadoLayout({
  children,
}: {
  children: ReactNode
}) {
  return children
}
